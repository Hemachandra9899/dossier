export const convertFilesTask = {
  id: "convert-files",
  trigger: async () => ({}),
  batchTrigger: async () => ({}),
  triggerAndWait: async () => ({}),
  triggerAndSubscribe: async () => ({}),
  batchTriggerAndWait: async () => ({}),
} as any;
export const convertFilesToPdfTask = convertFilesTask;
